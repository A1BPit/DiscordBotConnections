{
  "name": "discord-connections-bot",
  "version": "1.0.0",
  "description": "Discord bot for creating and playing Connections-style word puzzles",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "register": "node src/registerCommands.js",
    "dev": "concurrently -n bot,client \"node src/index.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "test": "node --test \"tests/*.test.js\""
  },
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
